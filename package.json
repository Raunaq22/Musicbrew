{
  "name": "musicbrew",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "cd server && npm install && (npx prisma generate --schema=./prisma/schema.prisma || true) && cd ../client && npm run build",
    "start": "node server/server.js",
    "dev": "concurrently \"npm --prefix server run dev\" \"npm --prefix client start\""
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}
